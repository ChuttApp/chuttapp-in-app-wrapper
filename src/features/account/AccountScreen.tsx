import { Screen } from '@chuttapp/in-app-react'
import React from 'react'

export function AccountScreen() {
  return (
    <Screen
      headerOptions={{
        title: "Account"
      }}
    >
      Account
    </Screen>
  )
}
