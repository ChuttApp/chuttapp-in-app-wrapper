import { Screen } from '@chuttapp/in-app-ui'
import React from 'react'

export function AccountScreen() {
  return (
    <Screen
      headerOptions={{
        title: "Account"
      }}
    >
      Account
    </Screen>
  )
}
